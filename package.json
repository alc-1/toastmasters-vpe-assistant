{
  "name": "toastmasters-vpe-tracker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^2.0.0"
  }
}
